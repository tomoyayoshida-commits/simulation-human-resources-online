# UI全面調整 作業手順書（v0.9 → v1.0）

> 作成 2026-09-04。着手前の合意用。出典は `Downloads\無題のスプレッドシート.xlsx`（2026-09-04 15:17）の
> ユーザビリティ・チェックリスト。**§1の齟齬5件に回答が出るまで Phase 1 以降に着手しない。**
>
> `assignment.ts` / `optimizer.ts` / `calcEngine.ts` の**数式・定数・アルゴリズムには触れない**（CLAUDE.md §9）。

## 0. 現状スコア（チェックリスト実測）

| 用途 | 項目数 | 通過 | 未達 |
|---|---|---|---|
| ① 1年初めの配置案を作る | 30 | 13 | **17** |
| ② 新規採用の検討 | 40 | 22 | **18** |
| 合計 | 70 | 35 | **35** |

通過要件は「メイン使用用途2つについて、データ入力からデータ出力まで支援できること」。
現状は**ちょうど半分**。未達35件の内訳は §3 のフェーズに全件を割り付けてある。

未達行（シートの行番号）
- ①：8, 12, 14, 20, 21, 22, 24, 25, 26, 27, 30, 31, 32, 33, 34, 35, 36
- ②：19, 22, 26, 28, 29, 30, 32, 33, 34, 39, 40, 41, 42, 43, 44, 45, 46, 47

---

## 1. 着手前に決めていただきたいこと（Phase 0）

シートの記述とコードを突き合わせて見つかった齟齬。**すべて回答が要る。**

### C-1. 課題1の「最適化：売上／利益」トグルを外してよいか ★**決定済み（2026-09-04）**

シート①16注記：「全社売上最大化に関しては売上・利益の変更ボタンはいらないと思われる」

- 現状：`compareTasks.ts:125` がカードごとに `最適化：[売上][利益]` を出し、**(課題×指標)8通り**から選ばせる。
- 衝突先：CLAUDE.md §8 に「#p4はカードごとの『最適化：売上／利益』で8通りから選ぶ」「テストは8通り全部を総当たりと突き合わせる」と明記済み。外すと7通りになり `test/compareTasks.test.ts` / `test/optimizer.test.ts` の前提が崩れる。
- 論点：課題1は「全社売上最大」なので利益トグルは意味が二重になる、という指摘は妥当。ただし §7-1 の辞書式目的関数の設計と対になっている。

**決定：トグルは外さない。課題1カードのトグルに説明を添えて、意味の二重さだけを解消する。**

- 8通りの選択肢・目的関数・テストの前提はいずれも変更しない（CLAUDE.md §7・§8 は不変）。
- 課題1カードでは「売上／利益」が*課題の切り替え*ではなく*同じ課題の指標の切り替え*であることを明示する
  （例：利益側に「全社利益の最大化に切り替わります」）。他の3課題のカードは現状のまま。
- 実装は **Phase 2-6**。表示文言だけの変更で、`optimizer.ts` には触れない。

### C-2. ①14 と ②14 で「画面遷移先を予測できる」の判定が割れている

①は 0、②は 1。両画面はパンくず（`navigation.ts` の `renderBreadcrumb`）とヘッダーを共有しており、**実装上の差は無い**。
どちらが実態か。①だけ 0 になった理由（#p4 固有の何か）があれば教えてください。

### C-3. ①30 と ②38「保存したデータを適切に出力できる」の判定が割れている

①は 0（注記「CSVはできる。PDF出力については適切なガイドが敷かれていない」）、②は 1。
出力画面 `#p7` は `exportPanel.ts` の1実装で、**保存元が #p4 か #p5 かで分岐しない**（同ファイル冒頭コメント）。
①の 0 が「PDFのガイド不足」を指すなら ②も 0 のはず。**①に揃える**という理解でよいか。

### C-4. ②の注記「18.0 18と同様の問題が見られる」の行番号

②18「制約を満たさない場合、次にどのような操作をすればいいかわかる」は **1**（通過）と記録されている。
通過項目に「同様の問題」と注記が付くのは矛盾。指している行番号が違う可能性がある。実際の対象行はどれか。
（注記の続き「エラー時、次に進む処理は隠すようにする」は Phase 1-4 として実装可能な指示として受け取っている。）

### C-5. ②19 の対応範囲 — #p5 を #p4 と同じ8通りに広げるか

シート②19：「A事業部の利益重視など具体的なニーズを中心に検討ができる」=0、注記「できない。売上最大化で丸められる」

- 現状（確定）：`#p5` は **課題1・指標=売上に固定**。`hiringFlow.ts` の `p5-open-bench` が `const task = 1` / `const metric = 'revenue' as const`、比較表示も `renderCompareHiring(base, add10, 1, params)` と第3引数が `1` 固定。
- #p4 は8通り選べるので、#p5 だけ1通りという非対称。
- 影響範囲：`compareHiring.ts` の表示、作業机の上下段Δ（`beforeBaseline`/`afterBaseline` の2回の `runOptimization`）、保存時の `task`/`metric`。**計算式は不変**だが波及は広い。
- **推奨**：Phase 7 として独立させ、他フェーズ完了後に単独で着手する。

---

## 2. 再調査不要な確定事実

以下はコードで確認済み。手順書の読み手は再調査しなくてよい。

### 2.1 実装バグ（挙動が明らかに誤り）

| # | 事象 | 該当箇所 | 原因 |
|---|---|---|---|
| B-1 | 取込エラー時に「ファイルを変更」「取り込みを解除」が消え、やり直せない | `compareFlow.ts` の `refreshCompareGate` | `$('p4-file-actions')?.toggleAttribute('hidden', !state.employees100)`。`importEmployees` がエラーで `employees=null` を返すと操作ボタンごと隠れる。**エラー時こそ必要なボタン** |
| B-2 | ロックのつけ外しでカード幅が伸縮する | `hiringWorkbenchPanel.ts` の `buildCardHtml` | `c.locked` のとき 🔒 を `wb-card-id` の中に足すため、行の文字数が変わって幅が動く。枠を常に確保すれば直る（CSSのみ） |

### 2.2 画面の現状（シートの指摘に対応する実装）

| 指摘 | 現状 |
|---|---|
| ①26/②12/②26「制約条件が未記載・オプション前に表示を」 | **#p4 の取込ステップにだけ**「配置の前提条件」カードがある（`index.html:101-117`）。全社最低売上は常時表示、適正人数・最低人数は `<details>「詳しく見る」` の中。**#p5 の取込ステップには同等の表示が無い**。作業机（#p4/#p5とも）には制約の常時表示が無く、最低人数は違反時に列見出しへ ⚠ が出るだけ |
| ①10「エラー表示と配置の前提条件は逆の位置にしたい」 | 現状の並びは 入力検証レポート(`index.html:92`) → 配置の前提条件(98) → エラー表(118)。エラー表が前提条件の**後ろ**にある |
| ①12/②12「計算条件の場所」 | #p4/#p5 とも `<details>「オプション」`（`index.html:132` / `213`）の中に `paramsOptions` が入る。1階層に全パラメータが同列で並ぶ |
| ①20/②22「作業机は機能名」 | **#p4 は既に動作名**「この配置の人員配置を調整する ▶」（`compareTasks.ts:190`）。**#p5 だけ機能名が露出**「この採用を作業机で調整する ▶」（`index.html:239`）。→ ②22 はラベル修正で直る |
| ①24/②32「誰がどこに移動したか」 | 異動内訳は集計行のみ。#p4 は `A→B 3名`（`workbenchPanel.ts` の `buildActionsHtml`）、#p5 は `採用→A 2名 ／ B→見送り 1名`（`hiringWorkbenchPanel.ts` の `diffLine`）。**個人が特定できない** |
| ①25/②33「移動させたくない人物を設定」 | **#p4 にロックは無い**（`workbench.ts` にロックの概念自体が無い）。#p5 は `lockBase` による**既存100名の一括ロックのみ**で、個人指定は無い |
| ①22/②30「何と比較しての＋−か」 | #p5 は2段Δで基準名を書いている（`hiringWorkbenchPanel.ts` の `buildStatHtml`：「採用前比」「最適解比」）。**#p4 は1段で基準名が無い**（`deltaText(現在, baseline)` のみ） |
| ①33/②41「保存データへの入り口」 | トップバーの「保存配置」ボタン（`index.html:40`）のみ。トップ画面の本文からは辿れない |
| ①34/②42「100人と110人の区別」 | 一覧の列は 名前／課題／全社売上／調整／状態／保存日時／保存者（`exportPanel.ts:53`）。**`RunSummary` は既に `kind:'hiring'`・`hiredIds`・`companyProfit`・`movedFromBaseline` を持つ**（`runStore.ts:41-55`）→ **区別も概要表示もスキーマ変更なしで実装できる** |
| ①35/②43「なぜ出力できないか」 | 出力ステップの文言は「この配置案は制約を満たしていません。記録として保存されていますが、出力はできません。」だけ（`exportPanel.ts:78`）。`RunSummary` は `feasible: boolean` しか持たないが、**`SavedRun` は `roster`/`assignment`/`params` を持つので開いた時点で再計算して内訳を出せる**（保存形式の変更は不要） |
| ①21/②29「顔写真機能を知らない」 | トップバーに「人材登録」ボタン（`index.html:39`）はあるが、作業机・比較画面からの誘導が無い |

### 2.3 直近の変更で既に部分的に手当て済み

コミット `a7e78c9`（2026-09-04）で、トップの入口ボタンを「必ずデータ取込から」に統一し、
前回の到達点へは「↳ 前回の続き」という別ボタンで入る形にした。起動時の自動遷移も廃止。

→ シート①8「配置案モードを選択できる」の注記「比較という体になっているため選択が難しい」と
①20 の注記「配置を調整するボタンを押すとそのまま作業机に飛ぶ→選択→実行とするべき」は、
**同じ『選択と実行を分ける』という原則**の話。Phase 2 はこの原則を作業机の起動にも広げる。

---

## 3. 実行手順（フェーズとゲート）

各フェーズ末で `npx tsc -b` → `npm run lint` → `npm test` を通す。**フェーズごとに1コミット。**
計算ロジック（`calcEngine`/`optimizer`/`assignment`/`whatif`/`workbench`/`hiringWorkbench`）に触れたフェーズは
`npm run snapshot` も実行する（CLAUDE.md §9）。

### Phase 0 — 前提の確定
§1 の C-1〜C-5 に回答を得る。**ゲート：5件すべてに回答があること。** 未回答のまま次へ進まない。

### Phase 1 — 実装バグと並び順（低リスク・挙動が明白）
| 手順 | 内容 | 該当 | 対象 |
|---|---|---|---|
| 1-1 | 取込エラー時も「ファイルを変更／取り込みを解除」を出す | B-1 | `compareFlow.ts` |
| 1-2 | ロックマークの枠を常に確保しカード幅を固定 | B-2 | `styles.css` |
| 1-3 | 入力検証エラー表を「配置の前提条件」より前へ | ①10注記 | `index.html` |
| 1-4 | エラーがある間は「次へ進む」を隠す（現在は disabled のみ） | ②18注記 | `index.html` / 両フロー |

**ゲート**：`npm test` 168件 pass 維持。計算に触れないので snapshot 不要。

### Phase 2 — 言葉と導線（遷移予測）
| 手順 | 内容 | 該当 |
|---|---|---|
| 2-1 | #p5 のボタンを動作名へ（「この採用を作業机で調整する」→ 動作を書く） | ②22 |
| 2-2 | 作業机の起動を「選択→実行」に揃える（`a7e78c9` と同じ原則） | ①20 |
| 2-3 | 比較画面・作業机から人材登録(#p6)への導線を置く | ①21 ②29 |
| 2-4 | 保存配置(#p7)への導線をトップ本文にも置く（ヘッダーと2箇所） | ①33 ②41 ①36 ②44 |
| 2-5 | 見出し部の強調を上げる | ①15注記 |
| 2-6 | 課題1カードのトグルに説明を添える（C-1 の決定。トグル自体は残す・文言のみ） | ①16注記 |

**ゲート**：C-2 の回答を反映してから ①14 の判定をやり直す。

### Phase 3 — 制約条件と前提パラメータの提示
| 手順 | 内容 | 該当 |
|---|---|---|
| 3-1 | #p5 取込ステップに「採用の前提条件」カードを追加（#p4 と同じ形） | ②12 ②26 |
| 3-2 | 作業机（#p4/#p5）に制約条件を常時表示（全社最低売上・事業部別最低人数） | ①26 ②34 |
| 3-3 | 「オプション」を二層化：結果を左右する値は前面、計算仕様に関わる値は奥へ | ①12注記 |
| 3-4 | 制約を満たさないときの次の一手を文章で示す | ①27 |

**ゲート**：`constants.ts` の値は変更しない（表示の追加のみ）。3-3 は `paramsOptions.ts` の
分類変更であり、`whatif.validateParams` の検証ロジックは変えない。

### Phase 4 — 数値の意味づけ
| 手順 | 内容 | 該当 |
|---|---|---|
| 4-1 | #p4 のΔに基準名を付ける（#p5 の2段Δと同じ作法に寄せる） | ①22 ②30 |
| 4-2 | 「最適解」が何との比較かを画面に書く／最適配置の姿を示す | ②28 |
| 4-3 | 貢献度の但し書き（または具体例）を添える | ①22注記 |
| 4-4 | 警告文から具体値を外す（非同期で値がずれるため） | ①27注記 |
| 4-5 | #p5 の比較表示に売上も出す（現在は利益のみ） | ②16注記 |

**ゲート**：4-1 は `workbenchView.ts` の共有部品に手が入る。`test/workbenchPanel.test.ts` /
`test/hiringWorkbenchPanel.test.ts` の25件が通ること。

### Phase 5 — 異動の可視化と個人ロック ★純粋関数に手が入る
| 手順 | 内容 | 該当 |
|---|---|---|
| 5-1 | 異動内訳を社員番号で示し、元の所属を色で表す | ①24 ②32 |
| 5-2 | 個人単位のロックを #p4/#p5 に入れる | ①25 ②33 |

**ゲート**：`workbench.ts` / `hiringWorkbench.ts` の純粋関数を変更するため、
**先にテストを追加してから実装する**。`npm run snapshot` で実データ4課題の結果が不変であること
（ロックは配置の探索に関わらない表示・操作の制約として実装する）。

### Phase 6 — 保存と出力
| 手順 | 内容 | 該当 |
|---|---|---|
| 6-1 | 一覧に概要列を足す（100名/110名の別・人数・見込み売上・最低人数/最低売上） | ①34 ②42 |
| 6-2 | 出力不可の理由を内訳で出す（どの制約をどれだけ割っているか） | ①35 ②43 |
| 6-3 | PDF保存時の注意点を出力画面の余白に書く | ①31 ②39 |
| 6-4 | 環境差（ブラウザ・OS）の出力検証と、差が出る場合の但し書き | ①32 ②40 |
| 6-5 | 顔写真の更新手段（編集モード）を用意する | ①37注記 ②45 |

**ゲート**：6-1・6-2 とも既存の保存データだけで実装できる（§2.2）。
**`runStore.ts` に更新・削除の関数を置かない**（追記のみという確定事項・CLAUDE.md §5）。
6-4 は実機確認が要るため、作業者（ユーザー）の確認待ちをフェーズ内に含める。

### Phase 7 — 採用判断の目的関数（C-5 の回答後）
#p5 を #p4 と同じ (課題×指標) から選べるようにする。`hiringFlow.ts` の `task=1`/`metric='revenue'` 固定を解く。
**ゲート**：計算式は不変。`npm run snapshot` で実データ4課題が不変であること。C-5 で「広げない」と決まった場合はフェーズごと削除。

### Phase 8 — 出力物の拡充
| 手順 | 内容 | 該当 |
|---|---|---|
| 8-1 | 新しく雇用された人が一目でわかる図を出力に含める | ②46 |
| 8-2 | エグゼクティブサマリを「何がどれくらい変化したか」中心に組み替える | ②47 |

**ゲート**：`exportDocs.ts` は別セッションが編集中の可能性があるため、着手前に `git status` を確認する。

---

## 4. 受入基準

1. チェックリスト70項目を再判定し、**通過 35 → 60 以上**（未達を35件から10件以下へ）。
   Phase 7・8 を実施しない場合は、その5件（②19/46/47・①30・②38）を除いた65項目で判定する。
2. Phase 1 の B-1・B-2 は再現手順で確認する。
   - B-1：不正なCSV（`~/development/資料/テストケース/形状01〜09`）を #p4 に投入 → エラー表示後も「ファイルを変更」「取り込みを解除」が押せること
   - B-2：#p5 作業机でロックのON/OFFを切り替え、カード幅が変わらないこと
3. 各フェーズ末で `npm test` が pass（現在168件）、`npm run lint` が 0 warnings / 0 errors。
4. Phase 5・7 の後は `npm run snapshot` が `docs/baseline-snapshot.txt` と一致すること。
5. `npm run build` → `firebase deploy` まで実行し、公開URL（https://summer011.web.app ）で作業者が確認する。

---

## 5. やらないこと

- **数式・定数・アルゴリズムの変更**。`calcEngine.ts` / `optimizer.ts` / `assignment.ts` には触れない（CLAUDE.md §9）。
  CLAUDE.md §7 の確定済み設計判断（辞書式目的関数・境界の帰属・丸め・適正人数据え置き・タイブレーク・決定性）はすべて対象外。
- **課題1の売上/利益トグルの削除**。C-1 で「外さない」と決定済み（2026-09-04）。8通りの選択肢は維持し、文言だけ足す。
- **顔写真の編集専用ページの新設**。シート①37注記の懸念（認証・権限）に同意する。既存画面に編集モードを足す形で対応する（6-5）。
- **`runStore.ts` への更新・削除関数の追加**。保存は追記のみという確定事項を崩さない。
- **Playwright等での実ブラウザ探査的動作確認**。Google認証でゲートされており本環境では実ログインできない（CLAUDE.md §9）。
  動作確認は `tsc -b` / `npm test` / `npm run build` の通過までとし、実機確認は作業者が本番URLで行う。
- **チェックリストに無い改善の追加**。気づいた点は本書に追記して合意を取ってから着手する。

---

## 6. リスクと巻き戻し

- フェーズ単位コミットのため、問題が出たフェーズだけ `git revert` できる。
- 別セッション（Opus/Sonnet併用）が同じチェックアウトを編集する。着手時に必ず `git status --short` を見る。
  2026-09-04 時点で `exportDocs.ts` / `reasonText.ts` / `firebase.json` / `test/exportDocs.test.ts` が
  別セッションにより未コミットのまま残っている。Phase 8 は特に衝突しやすい。
- Phase 5 は純粋関数に触れる唯一のフェーズ。テストを先に書き、`npm run snapshot` の一致を毎コミットで確認する。
- 表示層は 2026-09-04 のリファクタで `workbenchView.ts` / `workbenchDnd.ts` に共通化済み。
  **片方の画面だけ直すと共有部品を壊す**ので、#p4/#p5 の両方で確認する。
