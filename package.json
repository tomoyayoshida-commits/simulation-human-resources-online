{
  "name": "simulation-human-resources",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "test": "node --experimental-strip-types --test test/*.test.ts",
    "test:one": "node --experimental-strip-types --test",
    "test:e2e": "echo 'test:e2e は Playwright 移行待ちのため一時的に無効です（docs/web-firebase-plan.md 参照）' && exit 1",
    "snapshot": "node --experimental-strip-types test/snapshot.ts",
    "lint": "oxlint"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "highs": "^1.15.2",
    "oxlint": "^1.78.0",
    "typescript": "~6.0.3",
    "vite": "^8.2.1"
  }
}
